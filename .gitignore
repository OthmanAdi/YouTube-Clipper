# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/

# Node / Vite
node_modules/
dist/
*.tsbuildinfo

# User data (clip outputs and logs are user-private)
output/
logs/
config/.secrets.env

# OS
Thumbs.db
.DS_Store
desktop.ini

# IDE
.vscode/
.idea/
*.swp

# Whisper model cache (downloaded on first run, ~3 GB)
.cache/
models/

# Lock files (committed selectively below — keep uv.lock + package-lock.json)
!apps/daemon/uv.lock
!apps/extension/package-lock.json
bash.exe.stackdump

# Local AI agent files (project-specific Claude instructions, agent scratchpads)
CLAUDE.md
.claude/
AGENTS.md
GEMINI.md
.cursor/

# Local note-taking on the project itself (not user clip output)
NOTES.md
TODO.md
.notes/

# Mirrored agent memory (copied from ~/.claude/.../memory/)
.memory/

# Planning-with-files working notes (internal, not part of the product)
task_plan.md
findings.md
progress.md
